version: '3.8'
services:
  postgres:
    image: postgres:12.7
    restart: always
    ports:
      - 5432:5432
    environment:
      POSTGRES_USER: test_user
      POSTGRES_PASSWORD: password
      POSTGRES_DB: test_db
